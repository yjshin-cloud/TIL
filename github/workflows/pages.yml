name: Deploy VuePress v2 to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # ✅ devDependencies(= vuepress)까지 설치
      - name: Install deps (include dev)
        run: npm ci || npm install
        env:
          NODE_ENV: development

      # (선택) 설치 검증: devDeps 존재 여부 & vuepress 버전
      - name: Debug deps
        run: |
          node -v
          npm -v
          npm pkg get devDependencies
          npx --yes vuepress@next -v

      # ✅ 빌드에서만 prod + base='/TIL/' 플래그
      - name: Build VuePress (docs → docs/.vuepress/dist)
        run: npx --yes vuepress@next build docs
        env:
          NODE_ENV: production
          GITHUB_PAGES: "true"

      - name: Add .nojekyll
        run: touch docs/.vuepress/dist/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vuepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
